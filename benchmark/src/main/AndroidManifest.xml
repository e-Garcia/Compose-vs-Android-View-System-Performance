<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Note: Do not add permissions here. MacrobenchmarkRule will attempt to auto-grant
         any permissions declared in this manifest to the target app. If the target app
         doesn't declare those permissions, the tests will fail with "Failed to grant permissions".

         For Android 10+ (API 29+), benchmarks use scoped storage and don't need WRITE_EXTERNAL_STORAGE. -->
</manifest>
